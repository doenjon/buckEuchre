# ==============================================================================
# Buck Euchre Environment Configuration
# ==============================================================================
# 
# FOR DEVELOPMENT: Copy this file to .env and use as-is
#   cp env.example .env
#
# FOR PRODUCTION: Copy to .env and change ALL passwords and secrets!
# ==============================================================================

# ------------------------------------------------------------------------------
# Database Configuration
# ------------------------------------------------------------------------------
# This is configured for PostgreSQL running in Docker (see below for setup)
DATABASE_URL="postgresql://buckeuchre:dev_password_123@localhost:5432/buckeuchre"
DB_PASSWORD="dev_password_123"

# Start PostgreSQL in Docker:
# docker run -d --name buckeuchre-postgres \
#   -p 5432:5432 \
#   -e POSTGRES_USER=buckeuchre \
#   -e POSTGRES_PASSWORD=dev_password_123 \
#   -e POSTGRES_DB=buckeuchre \
#   postgres:16-alpine

# ------------------------------------------------------------------------------
# JWT Configuration
# ------------------------------------------------------------------------------
# Development secret - CHANGE IN PRODUCTION!
JWT_SECRET="dev_jwt_secret_for_buck_euchre_do_not_use_in_production_12345678"
JWT_EXPIRES_IN="24h"

# Generate production secret:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# ------------------------------------------------------------------------------
# Server Configuration
# ------------------------------------------------------------------------------
NODE_ENV="development"
PORT="3000"

# Backend will run at: http://localhost:3000
# Health check: http://localhost:3000/health

# ------------------------------------------------------------------------------
# CORS Configuration
# ------------------------------------------------------------------------------
# Allow requests from Vite dev server
CORS_ORIGIN="http://localhost:5173"

# For production, set to your domain:
# CORS_ORIGIN="https://yourdomain.com"

# ------------------------------------------------------------------------------
# WebSocket Configuration
# ------------------------------------------------------------------------------
# Frontend connects to backend WebSocket here
WS_URL="ws://localhost:3000"

# For production with domain:
# WS_URL="wss://yourdomain.com"

# ------------------------------------------------------------------------------
# Logging
# ------------------------------------------------------------------------------
LOG_LEVEL="info"
# Options: error, warn, info, debug

# ==============================================================================
# Quick Start for AI Agents
# ==============================================================================
#
# 1. Copy this file:
#    cp env.example .env
#
# 2. Start PostgreSQL:
#    docker run -d --name buckeuchre-postgres \
#      -p 5432:5432 \
#      -e POSTGRES_USER=buckeuchre \
#      -e POSTGRES_PASSWORD=dev_password_123 \
#      -e POSTGRES_DB=buckeuchre \
#      postgres:16-alpine
#
# 3. Install dependencies:
#    cd backend && npm install
#    cd ../frontend && npm install
#
# 4. Run Prisma migrations:
#    cd backend && npx prisma migrate dev
#
# 5. Start backend:
#    cd backend && npm run dev
#
# 6. Start frontend (in another terminal):
#    cd frontend && npm run dev
#
# 7. Access app:
#    Frontend: http://localhost:5173
#    Backend:  http://localhost:3000
#
# ==============================================================================
# Production Security Notes
# ==============================================================================
#
# BEFORE DEPLOYING TO PRODUCTION:
# 1. Generate new JWT_SECRET (see command above)
# 2. Change DB_PASSWORD to strong random password
# 3. Update DATABASE_URL with new password
# 4. Set CORS_ORIGIN to your actual domain
# 5. Set WS_URL to your actual domain (wss://)
# 6. Set NODE_ENV="production"
# 7. Never commit .env file to git (it's in .gitignore)
#
# ==============================================================================

