# ==============================================================================
# Buck Euchre Frontend - Environment Configuration
# ==============================================================================
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env:
#    cp frontend/.env.example frontend/.env
# 
# 2. For local development: Use as-is
# 3. For production: Update with your domain URLs
# 
# ==============================================================================

# ------------------------------------------------------------------------------
# Backend API Configuration
# ------------------------------------------------------------------------------
# URL for REST API endpoints
VITE_API_URL="http://localhost:3000"

# For production:
# VITE_API_URL="https://api.yourdomain.com"

# ------------------------------------------------------------------------------
# WebSocket Configuration
# ------------------------------------------------------------------------------
# URL for WebSocket connections
VITE_WS_URL="http://localhost:3000"

# For production (use wss:// for secure WebSocket):
# VITE_WS_URL="https://yourdomain.com"
# or
# VITE_WS_URL="wss://yourdomain.com"

# ------------------------------------------------------------------------------
# Application Configuration
# ------------------------------------------------------------------------------
# Application name (displayed in UI)
VITE_APP_NAME="Buck Euchre"

# Application version (optional)
VITE_APP_VERSION="1.0.0"

# ==============================================================================
# Production Checklist
# ==============================================================================
# 
# BEFORE DEPLOYING TO PRODUCTION, VERIFY:
# 
# [ ] VITE_API_URL points to production API server
# [ ] VITE_WS_URL uses secure WebSocket (wss://)
# [ ] Both URLs use HTTPS/WSS
# [ ] CORS is configured on backend to allow your domain
# [ ] API endpoints are accessible from production domain
# [ ] WebSocket connections work through any proxies/load balancers
# [ ] .env file is NOT committed to version control
# [ ] Environment variables are set in deployment platform (Vercel, Netlify, etc.)
# 
# ==============================================================================
