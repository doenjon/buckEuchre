# ==============================================================================
# Docker BuildKit Configuration
# ==============================================================================
#
# This file contains environment variables for optimizing Docker builds
# Copy to .env.buildkit and source before building:
#   cp .env.buildkit.example .env.buildkit
#   source .env.buildkit
#   docker-compose build
#
# ==============================================================================

# Enable BuildKit for faster, more efficient builds
DOCKER_BUILDKIT=1

# Enable BuildKit for docker-compose
COMPOSE_DOCKER_CLI_BUILD=1

# Optional: Enable buildx for multi-platform builds
# DOCKER_CLI_EXPERIMENTAL=enabled

# Optional: Configure build progress output
# BUILDKIT_PROGRESS=plain  # Show full build output
# BUILDKIT_PROGRESS=auto   # Auto-detect (default)
# BUILDKIT_PROGRESS=tty    # Interactive progress bar
