# ==============================================================================
# Buck Euchre - Development Environment Variables
# ==============================================================================
# 
# ⚠️  SECURITY WARNING - READ CAREFULLY:
# 
# - This file contains EXAMPLE passwords suitable for LOCAL DEVELOPMENT ONLY
# - NEVER use these passwords in production
# - NEVER commit actual passwords to version control
# - This file is tracked in git as an EXAMPLE only
# 
# FOR LOCAL DEVELOPMENT:
# - You can use these passwords as-is for local development
# - These credentials are for services running in Docker on your machine
# 
# FOR PRODUCTION:
# - Generate strong random passwords for all services
# - Use a secrets management system (AWS Secrets Manager, HashiCorp Vault, etc.)
# - Set NODE_ENV=production
# - Update all domains and URLs
# 
# ==============================================================================

# ------------------------------------------------------------------------------
# Docker Services Configuration
# ------------------------------------------------------------------------------
POSTGRES_DB=buckeuchre
POSTGRES_USER=buckeuchre
POSTGRES_PASSWORD=dev_password_change_in_prod
POSTGRES_PORT=5432

# ------------------------------------------------------------------------------
# Backend Application Configuration
# ------------------------------------------------------------------------------
# Update DATABASE_URL in backend/.env to match the above:
# DATABASE_URL="postgresql://buckeuchre:dev_password_change_in_prod@localhost:5432/buckeuchre"

# ==============================================================================
# Security Checklist for Production:
# ==============================================================================
# 
# [ ] Generate new POSTGRES_PASSWORD using: openssl rand -base64 32
# [ ] Generate new JWT_SECRET using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# [ ] Update DATABASE_URL with new password
# [ ] Set NODE_ENV="production" in backend/.env
# [ ] Set CORS_ORIGIN to your actual domain
# [ ] Use wss:// instead of ws:// for WebSocket URL
# [ ] Enable HTTPS/TLS on web server
# [ ] Configure firewall rules
# [ ] Set up database backups
# [ ] Enable database SSL connections
# [ ] Review and harden PostgreSQL configuration
# 
# ==============================================================================
